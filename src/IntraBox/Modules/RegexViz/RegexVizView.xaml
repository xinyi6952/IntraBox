<UserControl x:Class="IntraBox.Modules.RegexViz.RegexVizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:IntraBox.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <WrapPanel x:Name="ToolbarPanel" Margin="0,0,0,8">
            <Button Content="解释" Style="{StaticResource PrimaryButton}" Margin="0,4,8,4" Click="Run_Click"/>
            <TextBlock x:Name="MsgText" VerticalAlignment="Center" Margin="8,4,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </WrapPanel>
        <TextBlock x:Name="InputCaption" Grid.Row="1" Text="正则" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <controls:CodeEditor x:Name="PatternBox" Grid.Row="2"/>
        <controls:CodeEditor x:Name="ExplainBox" Grid.Row="3" Margin="0,8,0,0" IsReadOnly="True" CanMaximize="True"/>
        <StackPanel x:Name="SampleWrap" Grid.Row="4" Margin="0,8,0,0">
            <TextBlock Text="匹配示例" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock x:Name="SampleBox" FontFamily="Consolas" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
