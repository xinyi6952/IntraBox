<UserControl x:Class="IntraBox.Modules.Codec.CodecView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:IntraBox.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel x:Name="ToolbarPanel" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="类型" VerticalAlignment="Center" Margin="0,4,8,4"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <ComboBox x:Name="TypeCombo" MinWidth="200" Margin="0,4,12,4" SelectedIndex="0"
                      SelectionChanged="TypeCombo_SelectionChanged">
                <ComboBoxItem Content="URL 编码"/>
                <ComboBoxItem Content="URL 解码"/>
                <ComboBoxItem Content="HTML 编码"/>
                <ComboBoxItem Content="HTML 解码"/>
                <ComboBoxItem Content="Unicode 转义"/>
                <ComboBoxItem Content="Unicode 还原"/>
            </ComboBox>
            <Button Content="转换" Style="{StaticResource PrimaryButton}" Margin="0,4,8,4" Click="Convert_Click"/>
            <Button Content="复制" Margin="0,4,8,4" Click="Copy_Click"/>
            <TextBlock x:Name="MsgText" VerticalAlignment="Center" Margin="8,4,0,4"
                       Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
        </WrapPanel>

        <TextBlock x:Name="InputCaption" Grid.Row="1" Text="输入" Margin="0,0,0,4"
                   Foreground="{DynamicResource TextSecondaryBrush}"/>
        <controls:CodeEditor x:Name="InputBox" Grid.Row="2"/>

        <TextBlock x:Name="OutputCaption" Grid.Row="3" Text="输出" Margin="0,8,0,4"
                   Foreground="{DynamicResource TextSecondaryBrush}"/>
        <controls:CodeEditor x:Name="OutputBox" Grid.Row="4" IsReadOnly="True" CanMaximize="True"/>
    </Grid>
</UserControl>
