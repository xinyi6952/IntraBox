# IntraBox 功能代码评审清单

**版本**：v1.1（2026-09-06）  
**定位**：已落地功能的**防回归**评审（不是新需求设计评审）。  
**读者**：贡献者与 AI 助手。详细手工步骤仍以 [测试用例](测试用例.md) 为准。

| 修订日期 | 说明 |
|----------|------|
| 2026-09-06 | 初版：高发回归、P0 冒烟、分域检查与改动触发表 |

相关：[产品设计](产品设计文档.md) · [测试用例](测试用例.md) · [代码提交规范](代码提交规范.md) · [CONTRIBUTING](../CONTRIBUTING.md) · [AGENTS.md](../AGENTS.md)

---

## 1. 怎么用

| 项 | 约定 |
|----|------|
| **默认范围** | 当前未提交 diff，或 PR 相对默认分支的全部提交。点名了文件/模块则以点名为准。 |
| **节奏** | 功能告一段落或发版前做一次；改账号备忘 / 剪贴板 / 数据目录 / 导航注册时，当次必过对应专节 |
| **产出** | 结论（通过 / 部分 / 失败）+ 可复现问题（文件或行为）+ 是否建议补测试/文档 |
| **原则** | 以「用户可感知结果」验收，不以「能编译」代替；改落盘必看迁移与明文/密文边界 |

**执行顺序**

1. 看 diff 与 §5 触发表，只跑相关专节 + 对应 P0。  
2. 先过 §2 高发模式。  
3. 能跑的自动化先跑：`test.bat`。  
4. 交互项对照 `docs/测试用例.md` 相关条，不要把 44 个工具全点一遍。

---

## 2. 高发回归模式

| # | 模式 | 典型后果 | 自检一句 |
|---|------|----------|----------|
| R1 | **中文源码无 UTF-8 BOM** | 界面乱码（net48 按 ANSI 读源码） | 新增/改过的 `.cs`/`.xaml` 是否跑过 `tools/add-bom.ps1`？ |
| R2 | **托盘占用 exe** | `build.bat` 失败、跑到的仍是旧程序 | 是否先退出托盘 IntraBox 再构建？ |
| R3 | **账号备忘清空明文** | 勾选加密/复制后账号密码变空 | 加密是否只封存 Enc、界面掩码？复制是否先验证再出明文？ |
| R4 | **剪贴板把缩略图当原图 / Alpha 全 0** | 写回黑块、糊图 | 写回是否走原图 PNG+24 位 DIB？是否补不透明？ |
| R5 | **只改 UI 不改落盘/迁移** | 换数据目录丢数据、旧 JSON 读崩 | `DataPaths` 是否拷了新子目录？缺字段是否有缺省？ |
| R6 | **抽屉离开不确认** | 未保存内容丢失 | `ILeaveGuard` 是否仍在？自动保存开时是否 flush？ |
| R7 | **模块用完不释放** | 切工具内存只涨不回 | `OnDeactivated` 是否释放大图/WebBrowser/计时器？ |
| R8 | **查看密码会话范围错** | 关抽屉再进仍免验证，或同页反复弹窗 | 验证是否只在**本次打开编辑页**有效？ |
| R9 | **C# 8+ / 乱加 NuGet** | Win7 / net48 编不过或体积膨胀 | 是否仍 C# 7.3？新包是否必要？ |
| R10 | **文档不同步** | 按说明操作对不上 | 设计 / 测试用例 / 使用说明是否跟上行为？ |

---

## 3. P0 冒烟

> 构建前若托盘有 IntraBox，先退出。用 `build.bat` 的 `dist` 或当次 Release 产物验证。

### 3.1 启动与壳

- [ ] 启动单实例；二次启动唤醒已有窗口
- [ ] 关主窗口按设置进托盘或退出；托盘右键能打开/退出
- [ ] 导航默认：效率工具（剪贴板、启动器、任务计划、笔记、账号备忘）在前，设置在底
- [ ] 状态栏当前工具名正确；点「内存 n MB」能打开明细

### 3.2 构建与自动化

- [ ] 含中文的源码带 UTF-8 BOM（`tools/check-bom.ps1`）
- [ ] Release 能编过；`test.bat` **全绿**
- [ ] 未擅自升级语言版本、未无必要加 NuGet

### 3.3 剪贴板历史（有改动或发版前）

- [ ] 复制文本进列表；写回后不立刻多一条重复
- [ ] 复制截图：缩略图不是黑块；写回原图清晰
- [ ] 不打开该工具时复制，打开后仍能看到

### 3.4 账号备忘（有改动或发版前）

- [ ] 勾选加密：先设/验查看密码；界面不显示明文；复制能出明文
- [ ] 取消加密：未验证则先验证，再显示明文
- [ ] 本次打开编辑页验证一次即可；关闭后再进仍要验证
- [ ] 复制块为 `平台:` / 有则 `URL:` / `账号:` / `密码:`；复制全部 / 导出 / 导入往返
- [ ] 默认复制不进剪贴板历史

### 3.5 抽屉与离开

- [ ] 笔记 / 待办 / 账号备忘：未自动保存时关抽屉询问
- [ ] 切走模块再回来，输入/列表不丢

---

## 4. 分域检查

### 4.1 账号备忘（`Modules/Vault`、`Core/VaultCrypto`）

- [ ] 加密行落盘无明文；未勾选加密的行明文落盘（产品如此）
- [ ] 勾选加密立刻封存 Enc、界面掩码；空 Enc 时不覆盖内存明文
- [ ] 复制 / 复制全部 / 导出：加密行须已验证
- [ ] 导入按导出格式**追加**，不覆盖整篇；导入行默认不加密
- [ ] 「复制新增当前行」只带平台和 URL，账号密码空、不勾选加密
- [ ] 行内删除需再点「确认删除」；忘记查看密码无法解密（无后门、密钥不落盘）

### 4.2 剪贴板（`ClipboardImage`、`ClipboardHistory`）

- [ ] 禁止把缩略图当原图写回；DataObject 含 PNG 与 24 位 DIB；全 0 Alpha 补不透明
- [ ] 账号备忘复制默认不写入剪贴板历史
- [ ] 剪贴板占用：中文提示，不弹全局异常、不插入半截文本

### 4.3 笔记 / 待办

- [ ] 未保存离开确认；自动保存开时关抽屉 flush
- [ ] 笔记类型筛选与新增下拉仍对；待办提醒/导出关窗不泄漏资源
- [ ] 示例可删，启动时不要把用户改过的示例盖回去（除非产品规定刷新示例）

### 4.4 设置 / 数据目录 / 主题

- [ ] 改数据根会复制文件，禁止选到程序目录内；`vault\` `notes\` `todo\` 等一起迁
- [ ] 旧 JSON 缺字段有缺省；深/浅色切换后仍可读
- [ ] 工具显隐与导航排序：设置始终在底部；保存后侧栏立即变

### 4.5 模块注册与导航

- [ ] 新工具：`Modules/` 子目录 + `RegisterDefault` + `NavOrder`
- [ ] 按需加载、离开销毁；`ILeaveGuard` 切走会确认

### 4.6 构建 / 编码 / 测试

- [ ] C# 7.3；含中文源码 UTF-8 **带 BOM**
- [ ] 确定性逻辑优先 `Core/*Helper` + MSTest
- [ ] 行为变了则改设计文档、测试用例、必要时使用说明
- [ ] 不提交 `dist/`、`bin/`、密钥、用户数据目录

---

## 5. 改动触发表

diff 命中时，交付前必跑对应专节：

| 触发条件 | 必跑 |
|----------|------|
| `Modules/Vault/**`、`VaultCrypto`、`VaultText` | §3.4 §4.1 |
| `ClipboardImage` / `ClipboardHistory` / `ClipboardHelper` | §3.3 §4.2 |
| `Modules/Notes/**`、`Modules/Todo/**` | §3.5 §4.3 |
| `DataPaths` / `ConfigManager` / `AppSettings` / `Themes` | §4.4 |
| `ModuleRegistry` / `NavOrder` / `MainWindow` / `App.xaml` | §3.1 §4.5 |
| 新 `.cs`/`.xaml` 含中文、或 csproj 包变更 | §3.2 §4.6 |

**红线**

1. 账号备忘加密行禁止明文落盘；复制/导出前必须已验证。  
2. 剪贴板禁止用缩略图冒充原图；DIB Alpha 全 0 必须补不透明。  
3. 含中文源码必须 UTF-8 BOM。  
4. IntraBox 在托盘运行时不要假装构建成功。  
5. 禁止 C# 8+ 与未经说明的新 NuGet。

---

## 6. 建议补的自动化

| 优先级 | 用例 | 说明 |
|--------|------|------|
| P0 | CopyAll ↔ ParseExport | 已有；改格式须同步改测试 |
| P0 | PackForDisk 后无明文 | 已有 |
| P0 | 剪贴板 DIB/PNG 写回不黑 | 已有 `ClipboardImage` 一组 |
| P1 | 导入追加不覆盖已有行 | 可补「原有条数 +N」 |

---

## 7. 快速对照

```text
数据根（默认）：%LOCALAPPDATA%\IntraBox\Data
账号备忘 / 笔记 / 待办：Data\vault\  notes\  todo\
构建：build.bat → dist\IntraBox\
测试：test.bat
BOM：tools\add-bom.ps1 / tools\check-bom.ps1
```

账号备忘复制块（记录之间空一行）：`平台:` / 有则 `URL:` / `账号:` / `密码:`

---

## 8. 维护

1. 新踩「好好的被改坏」的坑 → 补 §2 + 对应 §4（一句话即可）。  
2. 新能力易回归 → §3 加 1～3 条冒烟，不要写成第二份测试用例。  
3. 产品行为变更改设计/用例/使用说明；本文只保防回归最小集。
