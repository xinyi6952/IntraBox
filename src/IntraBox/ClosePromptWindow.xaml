<Window x:Class="IntraBox.ClosePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="关闭提示" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" ShowInTaskbar="False"
        Background="{DynamicResource WindowBgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        FontSize="14">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,18">
            <Button DockPanel.Dock="Right" Content="×" Width="28" Height="28" MinWidth="28" MinHeight="28"
                    Padding="0" FontSize="16" ToolTip="关闭"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextSecondaryBrush}"
                    Click="TitleClose_Click"/>
            <TextBlock Text="您点击了关闭按钮，您想要：" VerticalAlignment="Center"
                       FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       TextWrapping="Wrap"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <RadioButton x:Name="TrayRadio" Content="最小化到托盘" GroupName="CloseAction"
                         Margin="0,0,0,14" FontSize="14"
                         Foreground="{DynamicResource TextPrimaryBrush}"/>
            <RadioButton x:Name="ExitRadio" Content="退出" GroupName="CloseAction"
                         IsChecked="True" FontSize="14"
                         Foreground="{DynamicResource TextPrimaryBrush}"/>
        </StackPanel>

        <DockPanel Grid.Row="2" LastChildFill="False">
            <CheckBox x:Name="DontAskCheck" DockPanel.Dock="Left" Content="不再提醒"
                      VerticalAlignment="Center"
                      Foreground="{DynamicResource TextPrimaryBrush}"/>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="取消" MinWidth="72" Margin="0,0,10,0"
                        IsCancel="True" Click="Cancel_Click"/>
                <Button Content="确认" MinWidth="72"
                        Style="{StaticResource PrimaryButton}"
                        IsDefault="True" Click="Ok_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
