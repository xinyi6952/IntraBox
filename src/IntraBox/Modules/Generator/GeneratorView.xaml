<UserControl x:Class="IntraBox.Modules.Generator.GeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:IntraBox.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="UuidTitle" Grid.Row="0" Text="UUID / ULID 生成" FontWeight="SemiBold" FontSize="14"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <WrapPanel x:Name="UuidBar" Grid.Row="1" Margin="0,8,0,6">
            <ComboBox x:Name="IdKindCombo" Width="110" Margin="0,4,12,4" SelectedIndex="0"
                      SelectionChanged="IdKind_Changed">
                <ComboBoxItem Content="UUID v4"/>
                <ComboBoxItem Content="ULID"/>
            </ComboBox>
            <TextBlock Text="数量" VerticalAlignment="Center" Margin="0,4,8,4"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox x:Name="UuidCountBox" Width="72" Margin="0,4,12,4" Text="5"/>
            <CheckBox x:Name="UuidUpperCheck" Content="大写" Margin="0,4,12,4"/>
            <CheckBox x:Name="UuidNoDashCheck" Content="去掉 '-'" Margin="0,4,12,4"/>
            <Button x:Name="GenIdBtn" Content="生成 UUID" Style="{StaticResource PrimaryButton}" Margin="0,4,8,4" Click="GenUuid_Click"/>
            <Button Content="复制" Margin="0,4,8,4" Click="CopyUuid_Click"/>
        </WrapPanel>
        <controls:CodeEditor x:Name="UuidOutput" Grid.Row="2" IsReadOnly="True" MinHeight="90" CanMaximize="True"/>

        <TextBlock x:Name="PwdTitle" Grid.Row="3" Text="密码生成" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,0"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <WrapPanel x:Name="PwdBar" Grid.Row="4" Margin="0,8,0,6">
            <TextBlock Text="长度" VerticalAlignment="Center" Margin="0,4,8,4"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox x:Name="PwdLengthBox" Width="72" Margin="0,4,12,4" Text="16"/>
            <TextBlock Text="数量" VerticalAlignment="Center" Margin="0,4,8,4"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBox x:Name="PwdCountBox" Width="72" Margin="0,4,16,4" Text="5"/>
            <CheckBox x:Name="UpperCheck" Content="大写" IsChecked="True" Margin="0,4,12,4"/>
            <CheckBox x:Name="LowerCheck" Content="小写" IsChecked="True" Margin="0,4,12,4"/>
            <CheckBox x:Name="DigitCheck" Content="数字" IsChecked="True" Margin="0,4,12,4"/>
            <CheckBox x:Name="SymbolCheck" Content="符号" IsChecked="True" Margin="0,4,16,4"/>
            <Button Content="生成密码" Style="{StaticResource PrimaryButton}" Margin="0,4,8,4" Click="GenPwd_Click"/>
            <Button Content="复制" Margin="0,4,8,4" Click="CopyPwd_Click"/>
        </WrapPanel>
        <controls:CodeEditor x:Name="PwdOutput" Grid.Row="6" IsReadOnly="True" MinHeight="90" CanMaximize="True"/>
    </Grid>
</UserControl>
