<UserControl x:Class="IntraBox.Modules.UnicodeInspect.UnicodeInspectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:IntraBox.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Margin="0,0,0,8">
            <Button Content="检查" Style="{StaticResource PrimaryButton}" Margin="0,4,8,4" Click="Run_Click"/>
            <Button Content="复制" Margin="0,4,8,4" Click="Copy_Click"/>
            <controls:GridFilterBar x:Name="FilterBar" VerticalAlignment="Center" Margin="8,4,0,4"/>
            <TextBlock x:Name="MsgText" VerticalAlignment="Center" Margin="8,4,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </WrapPanel>
        <TextBlock Grid.Row="1" Text="输入字符或字符串" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <controls:CodeEditor x:Name="InputBox" Grid.Row="2"/>
        <DataGrid x:Name="ResultGrid" Grid.Row="3" Margin="0,8,0,0" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="字符" Binding="{Binding Ch}" Width="60"/>
                <DataGridTextColumn Header="码点" Binding="{Binding Code}" Width="90"/>
                <DataGridTextColumn Header="UTF-8" Binding="{Binding Utf8}" Width="140"/>
                <DataGridTextColumn Header="UTF-16" Binding="{Binding Utf16}" Width="140"/>
                <DataGridTextColumn Header="HTML" Binding="{Binding Html}" Width="100"/>
                <DataGridTextColumn Header="类别" Binding="{Binding Cat}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
