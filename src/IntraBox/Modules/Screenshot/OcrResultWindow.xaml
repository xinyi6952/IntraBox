<Window x:Class="IntraBox.Modules.Screenshot.OcrResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 识别结果" Width="520" Height="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <DockPanel Margin="12">
        <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,0,0,8"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Text="识别结果可直接选中复制。本地 Tesseract（中英），无需联网。Windows 7 需已安装 VC++ 2015-2019 运行库。"/>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="复制全部" Style="{StaticResource PrimaryButton}" Click="Copy_Click" Margin="0,0,8,0"/>
            <Button Content="关闭" Click="Close_Click"/>
        </StackPanel>
        <TextBox x:Name="ResultBox" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                 FontFamily="Consolas, Microsoft YaHei UI" FontSize="13"/>
    </DockPanel>
</Window>
